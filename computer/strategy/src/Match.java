
public class Match {
	/*
	// The pitch grid
	private Pitch pitch = new Pitch();
	
	// Our pathfinder
	private PathFinder finder;
	
	// The path for our robot
	private Path path;
	
	new Robot ourRobot = ourRobot(0, 0, 0, 0, false);
	
	// Boolean for determining if we are in play or not
	private boolean playing = false;
	
	public Match(){
		
		finder = new AStarPathFinder(pitch, 500);
		while(playing = true && ourRobot.hasBall() == false){
			finder.findPath(ourRobot, ourRobot.getPosX, ourRobot.getPosY, Ball.getPosX, Ball.getPosY)
		}
	}
	
	
	
	public static void main (String argv[]){
		Match test = new Match();
	}
	
	*/
}	
